# Xiaomi folio keyboard (nanosic HID 15d9:00a3).
# Tap Shift → Hangul (fcitx5 toggle). Hold Shift → capitals.
# Modifier-only Shift through GTK/IBus is flaky on this I2C MCU.
[ids]
15d9:00a3

[main]
leftshift = overloadt(shift, hangul, 400)
rightshift = overloadt(shift, hangul, 400)
