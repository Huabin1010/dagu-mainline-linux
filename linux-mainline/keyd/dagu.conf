# Do not grab 15d9:00a3.
# keyd EVIOCGRAB sent folio letters through uinput without MSC_SCAN.
# Wayland then skipped hold-repeat on the first key (Bluetooth was fine).
# Tap Shift → fcitx5 is /usr/local/sbin/dagu-fcitx5-shift-tap (no grab).
